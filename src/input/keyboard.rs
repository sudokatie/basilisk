//! Keyboard event handling (stub)

pub struct KeyboardHandler {
    // Will track modifier state
}

impl KeyboardHandler {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for KeyboardHandler {
    fn default() -> Self {
        Self::new()
    }
}
